project:
  subprojects:
  - group: laplacian
    type: schema
    name: schema.project
    namespace: laplacian
    description: |
      A model which expresses the logical structure of laplacian-based projects and modules. 
    version: '1.0.0'
    base_dir: subprojects/laplacian.schema.project
    schemas:
    - group: laplacian
      name: model.metamodel
      version: '1.0.0'
    models:
    - group: laplacian
      name: model.project
      version: '1.0.0'
    templates:
    - group: laplacian
      name: template.project.base
      version: '1.0.0'
    - group: laplacian 
      name: template.entity.kotlin
      version: '1.0.0'

    
